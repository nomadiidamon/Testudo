    using UnityEngine;

    public static class RoomFactory
    {
        //public static Room CreateRandomRoom(Vector3 worldSize, int minWidth, int maxWidth, int minHeight, int maxHeight, int minLength, int maxLength)
        //{
        //    int width = Random.Range(minWidth, maxWidth);
        //    int length = Random.Range(minLength, maxLength);
        //    int height = Random.Range(minHeight, maxHeight);

        //    int xPos = Random.Range(0, (int)(worldSize.x - width));
        //    int zPos = Random.Range(0, (int)(worldSize.z - length));

        //    Vector3 position = new Vector3(xPos, 1, zPos);

        //    return new Room(position, width, height, length, RandomDungeonManager.Instance.roomFloorPrefab);
        //}

        //public static Room CreateRoom(Vector3 worldPos, int width, int height, int length)
        //{
        //    return new Room(worldPos, width, height, length, RandomDungeonManager.Instance.roomFloorPrefab);
        //}

    }

